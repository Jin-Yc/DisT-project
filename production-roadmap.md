# DisT 投产技术路径

## 目标

将当前本地原型升级为企业可用的平台，保留现有项目评审、Issue 闭环、项目确认与排期分发流程。

## 核心改造

| 领域 | 原型现状 | 投产方案 |
| --- | --- | --- |
| 身份与权限 | 浏览器本地切换角色 | 企业 SSO（OIDC/SAML）+ 项目级 RBAC |
| 业务数据 | SQLite | PostgreSQL + 审计日志 |
| 文件 | 本地模拟 | 对象存储，保存纪要、PRD、方案与导出文件 |
| AI | 模拟建议 | 后端调用企业模型或 Azure OpenAI |
| 协作 | 页面内操作 | Teams/邮件通知，Jira 或 Azure DevOps 任务同步 |
| 部署 | 本机 Flask | 容器化部署、CI/CD、监控告警 |

## AI 与 RAG

### 密钥管理

- API Key 仅保存在 Key Vault、Secrets Manager 或企业 Vault。
- 前端不保存 Key；后端通过服务身份调用模型。
- 配置额度、限流、超时、重试、调用日志与成本归集。

### RAG 数据

优先接入：

- 历史 PRD、最终方案、评审记录、Issue 与关闭方案。
- 方法论、数据口径、样本规范、运营上线与验收标准。
- 排期模板、风险库、团队职责和历史项目依赖。

每份文件需记录项目、版本、来源、拥有团队、生效日期、密级与访问权限。解析 PDF、DOCX、PPTX、Markdown、Excel 后，按章节和表格切块并写入向量库。

检索必须先做项目和权限过滤，再做语义搜索；回答应返回引用来源和版本。无可靠依据时，AI 必须明确说明无法判断。

## 安全原则

- SSO + 最小权限；角色由服务端确定，不使用 `localStorage` 作为权限依据。
- 全链路 HTTPS、数据加密、导出和关键操作审计。
- 对上传文件、提示词和模型输出执行敏感数据治理与内容安全检查。
- AI 只提供建议；Issue 关闭、评审结论、项目确认和排期分发仍须由授权人员操作。

## 实施顺序

1. PostgreSQL、对象存储、SSO、RBAC、审计日志。
2. 将现有工作流迁入正式后端，补齐 API 与端到端测试。
3. 建设文件上传、解析、版本与权限管理。
4. 上线带引用和权限过滤的 RAG，先覆盖项目问答和规范检索。
5. 接入 Teams/邮件、Jira/Azure DevOps 等协作系统。
6. 建立 CI/CD、监控、告警、成本治理、灰度发布与灾备。
